# 2026-09-17 업무일지

## GitHub 원격 저장소 연결 및 최초 푸시

- 기존에 로컬 저장소에는 빈 `Initial commit` 하나만 있고 실제 프로젝트 파일은 모두 미추적 상태였다.
- 원격 저장소 `https://github.com/jkchappydev/kafka-redis-rabbitmq.git`가 비어 있는 것을 확인하고(기존 커밋 덮어쓸 위험 없음) `origin`으로 연결했다.
- 사용자 요청에 따라 `master`를 그대로 메인(기본) 브랜치로 사용하기로 했다. `main`으로 이름을 바꾸지 않고 GitHub 기본 브랜치 설정을 `master`로 맞췄다.
- 프로젝트 초기 구성 전체(프런트엔드·백엔드·빌드 스크립트·문서)를 한 커밋으로 정리해 푸시했다.
- 관련 파일: `PROGRESS.md`, `docs/worklog/2026-09-17.md`, 그 외 저장소 전체 초기 구성 파일

## 메모

- `.gitignore`로 `node_modules/`, `build/`, `.gradle/`, `.cache/`가 제외되는 것을 확인했다. `frontend/package-lock.json`과 Gradle Wrapper(`gradlew`, `gradle-wrapper.jar`)는 의도적으로 포함했다.
- `.github/workflows/`와 `docs/`는 비어 있어 Git에는 올라가지 않는다. CI는 다음 작업으로 남겼다.
